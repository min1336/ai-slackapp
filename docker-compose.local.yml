services:
  slack-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: slack-bot-local
    env_file:
      - .env
    volumes:
      - ./credentials.json:/app/credentials.json:ro
