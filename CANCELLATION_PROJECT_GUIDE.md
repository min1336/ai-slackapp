# 결항확인서 검증 시스템 - 프로젝트 종합 가이드

> **대상 독자**: 비개발자 (운영팀, 기획자, 관리자)
> **최종 수정**: 2026-04-01

---

## 목차

1. [시스템 개요](#1-시스템-개요)
2. [기술 스택](#2-기술-스택)
3. [폴더 구조](#3-폴더-구조)
4. [데이터 구조](#4-데이터-구조)
5. [핵심 기능 설명](#5-핵심-기능-설명)
6. [워크플로우 (데이터 흐름)](#6-워크플로우-데이터-흐름)
7. [분석 알고리즘](#7-분석-알고리즘)
8. [설정 구조](#8-설정-구조)
9. [Slack 메시지 구성](#9-slack-메시지-구성)
10. [주요 파일별 함수 설명](#10-주요-파일별-함수-설명)

---

## 1. 시스템 개요

### 한줄 요약

**고객이 제출한 결항확인서 이미지를 AI가 자동 분석하고, 예약 정보와 교차검증하여 승인/반려를 판정하는 Slack 봇 시스템**

### 왜 만들었나

- 고객이 항공편 결항으로 렌트카 예약을 취소할 때, **결항확인서**를 제출해야 함
- 운영팀이 결항확인서를 **하나하나 열어서 날짜/이름/항공편을 확인**하는 데 시간이 많이 걸림
- 이 시스템이 **설문 접수 → 이미지 수집 → AI 분석 → 예약 대조 → 승인/반려 판정**까지 자동으로 처리해줌

### 누가 사용하나

| 사용자 | 하는 일 | 접속 방식 |
|--------|---------|-----------|
| **운영팀** | Slack에서 결항 건 처리 결과 확인, 운영현황 시트에서 현황 관리 | Slack + Google Sheets |
| **고객** | Jotform 설문으로 결항확인서 이미지 제출 | Jotform 설문 링크 |
| **자동 시스템** | 설문 접수 감지 → 이미지 수집 → AI 분석 → 결과 포스팅 | Slack 봇 (자동) |

**핵심 업무 흐름:** 고객이 설문 제출 → 봇이 자동 처리 → 운영팀은 Slack에서 결과만 확인

### 핵심 숫자

| 항목 | 내용 |
|------|------|
| AI 분석 모델 (Primary) | Gemini 2.5 Flash |
| AI 분석 모델 (Fallback) | GPT-4o-mini |
| 이미지 제한 | 최대 10장, 각 4MB 이하 |
| 날짜 검증 허용 범위 | 대여기간 전후 1일 |
| 자동 재시도 | 최대 2회 (30초, 60초 간격) |

---

## 2. 기술 스택

> "이 시스템을 만드는 데 어떤 도구를 썼나?"

### 쉬운 비유

| 역할 | 사용 기술 | 비유 |
|------|-----------|------|
| **Slack 봇** | Slack Bolt (Socket Mode) | 사무실의 비서 - 메시지를 감시하고 알아서 처리 |
| **설문 시트** | Google Sheets (gspread) | 접수 대장 - 고객 신청 내역이 쌓이는 곳 |
| **이미지 저장소** | Google Drive | 서류함 - 결항확인서 사진이 보관된 곳 |
| **AI 분석 (주력)** | Gemini Flash | 전문 심사관 - 서류를 읽고 정보를 추출 |
| **AI 분석 (예비)** | GPT-4o-mini | 보조 심사관 - 주력이 바쁠 때 대신 처리 |
| **예약 정보** | Slack 채널 메시지 + DB 캐시 | 예약 원장 - 고객 예약 내역이 기록된 곳 |
| **PDF 변환** | pdf2image (Poppler) | 스캐너 - PDF를 이미지로 변환해서 AI가 읽을 수 있게 |

### 상세 기술 목록

| 분류 | 기술 | 용도 |
|------|------|------|
| 언어 | Python 3.12 | 전체 코드 작성 |
| Slack 프레임워크 | Slack Bolt (Socket Mode) | Slack 이벤트 수신, 메시지 전송 |
| 스프레드시트 | gspread 6.x | Google Sheets 읽기/쓰기 (설문, 운영현황) |
| 클라우드 저장소 | Google Drive API v3 | 결항확인서 이미지 검색, 다운로드 |
| AI (Primary) | Google Gemini Flash API | 이미지에서 텍스트 추출, 문서 유효성 검증 |
| AI (Fallback) | OpenAI GPT-4o-mini Vision | Gemini 과부하 시 대체 분석 |
| PDF 처리 | pdf2image + Poppler | PDF → 이미지 변환 (AI 분석 전처리) |
| DB 캐시 | PostgreSQL (Supabase) | 예약 스레드 위치 캐시 (검색 속도 향상) |
| 로깅 | structlog | 구조화된 로그 (JSON/컬러 콘솔) |

---

## 3. 폴더 구조

> "결항 시스템 관련 파일들이 어디에 있나?"

```
ai-slackapp/
│
├── app/
│   ├── models/                           # 데이터 형태 정의
│   │   ├── cancellation.py               #   결항 도메인 모델 3개
│   │   └── analysis.py                   #   AI 분석 결과 모델 3개
│   │
│   ├── services/                         # 비즈니스 로직
│   │   ├── cancellation_image_service.py  #   [핵심] 전체 워크플로우 조율
│   │   ├── image_analyzer.py              #   AI 이미지 분석 (프롬프트 + 파싱)
│   │   ├── drive_file_collector.py        #   Drive 파일 수집 + PDF 변환
│   │   ├── reservation_locator.py         #   예약 스레드 검색 (DB→Slack 폴백)
│   │   └── pdf_converter.py               #   PDF → 이미지 변환
│   │
│   ├── infrastructure/                   # 외부 서비스 연결
│   │   ├── protocols.py                  #   인터페이스 정의 (테스트 가능한 설계)
│   │   ├── survey_sheet.py               #   Google Sheets 읽기/쓰기
│   │   ├── drive_client.py               #   Google Drive 검색/다운로드
│   │   ├── gemini_client.py              #   Gemini Flash API 호출
│   │   ├── openai_client.py              #   GPT-4o-mini Vision 호출
│   │   └── fallback_gateway.py           #   Primary/Fallback 자동 전환
│   │
│   ├── views/                            # Slack 메시지 포맷
│   │   └── analysis.py                   #   분석 결과 Block Kit 포맷
│   │
│   ├── listener/                         # Slack 이벤트 수신
│   │   └── messages.py                   #   결항 채널 메시지 감지 → 폴링 트리거
│   │
│   └── container.py                      # 의존성 조립 (모든 컴포넌트 연결)
│
├── config.dev.yaml                       # 개발 환경 설정
├── config.prod.yaml                      # 운영 환경 설정
│
└── tests/
    ├── unit/
    │   ├── test_cancellation_image_service.py  # 워크플로우 테스트
    │   ├── test_image_analyzer.py              # AI 분석 테스트
    │   └── test_drive_file_collector.py        # 파일 수집 테스트
    └── fakes/
        ├── fake_survey.py                # 가짜 설문 시트 (테스트용)
        ├── fake_drive.py                 # 가짜 Drive (테스트용)
        └── fake_gemini.py                # 가짜 AI (테스트용)
```

### 계층 구조 (누가 누구를 호출하나)

```
listener/messages.py          ← Slack에서 메시지가 오면 감지
    ↓
CancellationImageService      ← 전체 흐름을 조율하는 "지휘자"
    ├── SurveySheetReader      ← 설문 시트에서 접수 내역 읽기
    ├── DriveFileCollector      ← Drive에서 이미지 수집
    │   ├── DriveImageClient    ←   실제 Drive API 호출
    │   └── PdfConverter        ←   PDF → 이미지 변환
    ├── ImageAnalyzer           ← AI에게 이미지 분석 요청
    │   └── GeminiClient        ←   Gemini API 호출
    │       └── FallbackGateway ←     실패 시 OpenAI로 전환
    ├── ReservationLocator      ← 예약 스레드 검색
    └── SlackWriter             ← Slack에 결과 메시지 전송
```

---

## 4. 데이터 구조

> "데이터가 어떻게 저장되어 있나?"

이 시스템은 데이터베이스 대신 **Google Sheets**를 주 저장소로 사용합니다.

### 시트 1: 설문 원본 시트 (Jotform 연동)

고객이 Jotform 설문을 제출하면 자동으로 이 시트에 행이 추가됩니다.

| 컬럼 | 설명 | 예시 |
|------|------|------|
| Submission ID | 설문 응답 고유번호 | 6170835291234 |
| Submission Date | 접수 일시 | 2026-03-20 14:30:00 |
| 1. 운전자 성함을... | 고객 이름 | 홍길동 |
| 2. 취소 및 환불 접수하실... | 예약번호 | ABC12345 |
| 업체명 | 렌트카 업체 | 롯데렌터카 |
| 전화번호 | 고객 연락처 | 010-1234-5678 |
| 처리완료 | 봇이 처리 완료 표시 | TRUE |

### 시트 2: 운영현황 시트 (봇이 자동 생성)

봇이 처리한 결과를 기록하는 운영 관리용 시트입니다.

| 컬럼 | 누가 채우나 | 설명 |
|------|------------|------|
| 작성일자 | 봇 (자동) | 설문 접수 일시 |
| 작성자 | 운영팀 (수기) | 담당자 이름 |
| 예약번호 | 봇 (자동) | 고객 예약번호 |
| 연락처 | 봇 (자동) | 고객 전화번호 |
| 고객명 | 봇 (자동) | 고객 이름 |
| 업체명 | 봇 (자동) | 렌트카 업체명 |
| 결항확인서 확인여부 | 운영팀 (수기) | 확인 완료 여부 |
| 환불 진행여부 | 운영팀 (수기) | 환불 처리 상태 |
| 업체 전달여부 | 운영팀 (수기) | 업체에 전달했는지 |
| 결항일자 | 봇 (AI 분석) | AI가 추출한 결항 날짜 |
| 항공편/선편 | 봇 (AI 분석) | AI가 추출한 항공편명 |
| 결항사유 | 봇 (AI 분석) | AI가 추출한 결항 사유 |
| 발급기관 | 봇 (AI 분석) | AI가 추출한 발급 기관 |
| AI요약 | 봇 (AI 분석) | AI가 생성한 1~2줄 요약 |
| 교차검증결과 | 봇 (자동) | 승인 / 반려 / 보류 |
| 교차검증사유 | 봇 (자동) | 판정 사유 |

### 데이터 모델 (코드 내부)

| 모델 | 역할 | 비유 |
|------|------|------|
| **SurveySubmission** | 설문 응답 1건 | 접수증 - 누가, 어떤 예약번호로 신청했는지 |
| **DriveFile** | Drive 파일 1개 | 서류 봉투 - 파일 이름, 종류(이미지/PDF) |
| **AnalysisResult** | AI 분석 결과 | 심사 보고서 - 추출된 정보, 요약, 부적합 사유 |
| **ReservationData** | 예약 정보 | 예약 원장 - 고객명, 대여기간, 결제금액 |
| **CrossVerificationResult** | 교차검증 판정 | 최종 판결문 - 승인/반려/보류 + 사유 |
| **FieldComparison** | 필드별 비교 | 대조표 - 문서 값 vs 예약 값, 일치 여부 |

### 데이터 관계도

```
[고객 설문 제출]         [Google Drive]          [Slack 예약 채널]
Jotform 설문 ──────→ 설문 시트               예약 메시지
      │              (접수 내역)                    │
      └───────────→ Drive 폴더                     │
                    (이미지 파일)                    │
                         │                         │
                         v                         v
                  [봇이 자동 처리]          [예약 스레드 검색]
                  이미지 다운로드            예약번호로 검색
                         │                         │
                         v                         v
                  [AI 이미지 분석]          [예약 데이터 파싱]
                  Gemini / GPT              대여기간 추출
                         │                         │
                         v                         v
                  [교차검증] ←─────────────────────┘
                  결항일 vs 대여기간
                         │
                    ┌────┼────┐
                    v    v    v
                  승인  반려  보류
                    │
                    v
             [운영현황 시트 기록]
             + [Slack 결과 포스팅]
```

---

## 5. 핵심 기능 설명

### 기능 1: 설문 접수 감지

> "고객이 결항확인서를 제출하면 봇이 알아서 감지한다"

**작동 방식:**
1. 고객이 Jotform 설문을 제출하면, 응답이 Google Sheets에 자동 저장됨
2. 동시에 Slack 결항 채널에 알림 메시지가 올라옴
3. 봇이 이 메시지를 감지하고, 5초 뒤 설문 시트를 확인함 (Jotform→시트 동기화 대기)
4. 미처리 건이 있으면 처리 시작

**같은 예약번호로 여러 번 제출한 경우:**
- 가장 최근 제출만 처리하고, 이전 제출은 "처리완료"로 마킹

---

### 기능 2: 이미지 수집

> "결항확인서 사진을 자동으로 찾아서 다운로드한다"

**작동 방식:**
1. 예약번호로 Google Drive 폴더를 검색 (예: "ABC12345" 폴더)
2. 폴더 안의 이미지/PDF 파일 목록을 가져옴
3. 파일을 하나씩 다운로드
4. PDF 파일은 이미지로 변환 (AI가 읽을 수 있도록)
5. 다운로드한 이미지를 Slack 결항 스레드에 업로드

**안전 장치:**
- 폴더나 파일이 아직 없으면 (Drive 동기화 지연) → 30초, 60초 후 재시도
- 일부 파일 다운로드 실패 → 성공한 파일만으로 계속 진행
- 모든 다운로드 실패 → 다음 폴링에서 재시도

---

### 기능 3: AI 이미지 분석

> "AI가 결항확인서 사진을 읽고 핵심 정보를 자동 추출한다"

**작동 방식:**
1. 이미지를 AI (Gemini Flash)에게 전송
2. AI가 이미지에서 정보 추출: 고객명, 예약번호, 날짜, 항공편, 결항사유, 발급기관
3. 문서 유효성 검증: 흐린 이미지, 지연 안내(결항이 아님), 고객명 누락 등 확인
4. 추출한 정보를 Slack 스레드에 포스팅
5. 운영현황 시트에도 분석 결과 기록

**부적합 판정 시:**
- Slack 스레드에 X 리액션 추가
- 부적합 사유를 메시지로 포스팅
- 교차검증은 진행하지 않음

---

### 기능 4: 교차검증

> "결항확인서의 날짜가 실제 예약 기간에 포함되는지 자동 대조한다"

**작동 방식:**
1. 예약번호로 예약 채널의 스레드를 검색 (DB 캐시 → Slack 히스토리 순서)
2. 예약 메시지에서 대여기간(시작일~종료일)을 파싱
3. AI가 추출한 결항일과 대여기간을 비교
4. 판정 결과를 Slack과 시트에 기록

**판정 기준:**

| 판정 | 조건 | 예시 |
|------|------|------|
| **승인** | 결항일이 대여기간 ±1일 이내 | 대여 3/15~3/20, 결항일 3/16 |
| **반려** | 결항일이 대여기간 밖 | 대여 3/15~3/20, 결항일 4/01 |
| **보류** | 날짜를 파싱할 수 없거나, 예약 스레드를 찾지 못함 | 날짜 형식 불명 |

**승인 시 추가 동작:**
- 결항 스레드에 예약 스레드 링크를 포스팅
- 예약 스레드에 결항 스레드 링크를 포스팅
- 운영팀이 양쪽을 쉽게 오갈 수 있게 양방향 연결

---

### 기능 5: 해외 결항 건 처리

> "해외(일본 등) 예약의 결항 건은 별도로 담당자에게 알린다"

**작동 방식:**
1. 예약번호 접두사로 해외 건 판별 (OT, HG, KL, IM 등)
2. 해외 건이면:
   - 담당자 그룹을 멘션하여 알림 메시지 포스팅
   - 일본 국기 리액션 추가
3. AI 분석은 진행하지 않음 (해외 건은 별도 프로세스)

---

## 6. 워크플로우 (데이터 흐름)

> "데이터가 처음부터 끝까지 어떻게 흘러가는지"

### 전체 흐름 요약

```
[1] 고객 접수          [2] 자동 수집           [3] AI 분석           [4] 결과
──────────────       ──────────────        ──────────────       ──────────────
고객이 Jotform       봇이 자동으로:         AI(Gemini)가:        운영팀이 확인:
설문 제출            - Drive 이미지 수집    - 문서 정보 추출     - Slack 스레드
     │               - Slack에 업로드       - 유효성 검증        - 운영현황 시트
     v                    │                - 예약 교차검증
설문 시트에               v                      │
응답 저장           이미지 업로드                  v
     │              완료                    판정 결과
     v                                     (승인/반려/보류)
Slack 결항 채널에                                │
알림 도착                                        v
     │                                     시트 + Slack에
     v                                     결과 기록
봇이 감지!
```

### 상세 단계별 흐름

| 단계 | 이름 | 하는 일 | 실패 시 |
|------|------|---------|---------|
| **0** | 감지 | Slack 결항 채널에 메시지 도착 → 5초 대기 후 폴링 시작 | - |
| **1** | 설문 읽기 | 시트에서 미처리 건 조회, 같은 예약번호 중복 제거 | 다음 폴링에서 재시도 |
| **2** | 스레드 찾기 | 결항 채널에서 예약번호로 스레드 검색 | 건너뜀 (다음 폴링에서 재시도) |
| **3** | 이미지 수집 | Drive에서 폴더 찾기 → 파일 다운로드 → PDF 변환 | 건너뜀 (30/60초 후 재시도) |
| **4** | 업로드 | 이미지를 Slack 스레드에 업로드 | 다음 폴링에서 재시도 |
| **5** | 시트 기록 | 운영현황 시트에 행 추가 (중복 체크) | 업로드는 이미 완료 |
| **6** | 해외 확인 | 예약번호 접두사로 해외 건 판별 | - |
| **6a** | (해외인 경우) | 담당자 멘션 + 리액션 → **종료** | 멘션 실패해도 계속 |
| **7** | AI 분석 | Gemini에 이미지 전송 → 정보 추출 + 유효성 검증 | 업로드는 이미 완료, 분석만 누락 |
| **7a** | (부적합인 경우) | X 리액션 + 부적합 사유 포스팅 → **종료** | - |
| **8** | 결과 포스팅 | 분석 결과를 Slack 스레드에 포스팅 | 시트 기록은 계속 |
| **9** | 시트 기록 | 운영현황 시트에 AI 분석 결과 기록 | - |
| **10** | 교차검증 | 예약 스레드 검색 → 날짜 비교 → 판정 | 분석 결과는 이미 포스팅됨 |
| **11** | 판정 기록 | 시트 + Slack에 판정 결과 기록, 승인 시 양방향 링크 | - |
| **12** | 완료 마킹 | 원본 시트의 "처리완료" 컬럼에 TRUE | - |

### 실제 예시: 결항 확인서 1건의 처리 과정

```
[입력] 고객 "홍길동"이 예약번호 "ABC12345"로 설문 제출
       첨부: 제주항공 결항확인서 이미지 1장 (Drive 자동 업로드)

  0단계: Slack #결항 채널에 Jotform 알림 메시지 도착
  → 봇이 감지, 5초 대기

  1단계: 설문 시트 조회
  → 미처리 건 1건 발견: 홍길동 / ABC12345

  2단계: #결항 채널에서 "ABC12345" 텍스트로 스레드 검색
  → 스레드 발견 (ts: 1711012345.123456)

  3단계: Drive에서 "ABC12345" 폴더 검색
  → 폴더 발견 → 이미지 1장 다운로드 (result_1.jpg, 1.2MB)

  4단계: 이미지를 스레드에 업로드
  → 업로드 성공

  5단계: 운영현황 시트에 행 추가
  → [2026-03-20, , ABC12345, 010-1234-5678, 홍길동, 롯데렌터카, , , ]

  6단계: 해외 건 확인
  → "ABC"는 해외 접두사(OT, HG, KL, IM)에 해당하지 않음 → 국내 건

  7단계: AI 분석 (Gemini Flash)
  → 추출 결과:
     고객명: 홍길동
     예약번호: ABC12345
     날짜: 2026-03-18
     항공편: 7C112
     결항사유: 기상악화
     발급기관: 제주항공
  → 부적합 사유 없음 → 유효한 문서

  8단계: 분석 결과 Slack 포스팅
  → 스레드에 Block Kit 메시지로 추출 정보 표시

  9단계: 운영현황 시트에 AI 결과 기록
  → 결항일자: 2026-03-18, 항공편: 7C112, ...

  10단계: 교차검증
  → 예약 채널에서 "ABC12345" 스레드 검색
  → 대여기간: 2026-03-17 ~ 2026-03-22 파싱 성공
  → 결항일 2026-03-18이 대여기간(3/16~3/23 허용) 내 → "승인"

  11단계: 판정 기록
  → 운영현황 시트: 교차검증결과=승인, 교차검증사유=날짜 검증 통과
  → 결항 스레드에 예약 스레드 링크 포스팅
  → 예약 스레드에 결항 스레드 링크 포스팅

  12단계: 원본 시트 "처리완료" = TRUE

[결과]
  운영팀은 Slack에서:
  - 결항확인서 이미지
  - AI 분석 결과 (추출된 정보)
  - 교차검증 판정 (승인)
  - 예약 스레드로 바로 이동할 수 있는 링크
  를 확인할 수 있음
```

---

## 7. 분석 알고리즘

> "AI가 결항확인서를 어떻게 분석하는지 상세 원리"

### 7-1. 이미지 전처리

```
[이미지 목록 수신]
     │
     v
[1] 크기 필터링
    - 4MB 초과 이미지 → 제외 (품질 이슈로 기록)
    - 10장 초과 시 → 처음 10장만 사용
     │
     v
[2] PDF 감지
    - PDF 파일이 있었으면 → :pdf: 리액션 추가 (운영팀 인지용)
     │
     v
[결과] 분석 가능한 이미지 목록 (최대 10장, 각 4MB 이하)
```

### 7-2. AI 이미지 분석

```
[이미지 + 프롬프트]
     │
     v
[1] 프롬프트 구성
    - 오늘 날짜, 예약번호, 고객명 포함
    - 추출할 필드 목록: 고객명, 예약번호, 날짜, 항공편, 결항사유, 발급기관
    - 날짜 형식 변환 규칙 (GDS 형식: 18MAR26 → 2026-03-18)
    - 유효성 검증 규칙 4가지
     │
     v
[2] AI 호출 (Gemini Flash)
    - temperature=0.1 (일관된 결과를 위해 낮게 설정)
    - JSON 형식으로 응답 요청
    - 타임아웃: 60초
     │
     ├─ 성공 → JSON 파싱
     │
     └─ 실패 (Rate limit) → Fallback (GPT-4o-mini)으로 재시도
         └─ 실패 (기타 에러) → 분석 포기, 이미지 업로드만 유지
     │
     v
[3] 응답 파싱
    - extracted_fields: 추출된 key-value 쌍
    - summary: 1~2문장 한국어 요약
    - rejection_reasons: 부적합 사유 목록 (빈 배열이면 유효)
     │
     v
[결과] AnalysisResult (추출 정보 + 요약 + 유효성 판정)
```

### 7-3. 유효성 검증 규칙

AI가 다음 4가지를 확인합니다:

| 검증 항목 | 부적합 기준 | 적합 기준 |
|-----------|------------|-----------|
| **이미지 품질** | 흐리거나 텍스트를 읽을 수 없음 | 글자가 읽힘 |
| **문서 종류** | 지연(delay) 안내 문서 | 결항(cancellation) 확인서 |
| **고객명** | 문서 어디에도 사람 이름이 전혀 없음 | 어떤 형태든 이름이 보임 |
| **날짜** | 날짜가 완전히 누락됨 | 월/일이라도 읽을 수 있음 |

### 7-4. 교차검증 알고리즘

```
[AI 추출 날짜] + [예약 대여기간]
     │
     v
[1] 예약 스레드 검색
    - DB 캐시에서 예약번호로 조회 (빠름)
    - 없으면 → Slack 채널 히스토리에서 검색 (느리지만 확실)
    - 전화번호 폴백: 예약번호로 못 찾으면 전화번호로 재검색
     │
     ├─ 찾지 못함 → "보류" (예약 스레드를 찾지 못했습니다)
     │
     └─ 찾음 → 메시지 텍스트에서 대여기간 파싱
          │
          v
[2] 날짜 비교
    - AI가 추출한 "날짜" 필드를 파싱
    - 지원 형식: YYYY-MM-DD, YYYY.MM.DD, YYYY/MM/DD, YYYY년 MM월 DD일
     │
     ├─ 파싱 실패 → "보류" (날짜 파싱 실패)
     │
     └─ 파싱 성공
          │
          v
[3] 범위 판정
    - 허용 범위: 대여시작일 -1일 ~ 대여종료일 +1일
    - 범위 내 → "승인"
    - 범위 밖 → "반려" (결항일이 대여기간 밖)
     │
     v
[결과] CrossVerificationResult (승인/반려/보류 + 사유)
```

---

## 8. 설정 구조

> "시스템을 어떻게 설정하나?"

### 설정 파일 위치

| 파일 | 용도 |
|------|------|
| `.env` | Slack 토큰, Google 자격 증명, AI API 키 (비밀 정보) |
| `config.dev.yaml` | 개발 환경 설정 (테스트용 채널, 시트) |
| `config.prod.yaml` | 운영 환경 설정 (실제 채널, 시트) |

### config.yaml 결항 섹션 구조

```yaml
cancellation:
  slack_channels:
    target: "C0AANUJGPAB"              # 결항 접수 Slack 채널 ID

  drive:
    parent_folder_id: "1mDpVCO..."     # Drive 상위 폴더 ID (여기 아래서 검색)

  spreadsheet:
    id: "1rp-ILKY..."                  # Google Sheets 문서 ID
    survey_sheet_name: "test"           # Jotform 설문 원본 시트 탭 이름
    formatted_sheet_name: "운영현황"     # 운영현황 시트 탭 이름

  analysis:
    enabled: true                       # AI 분석 켜기/끄기
    gemini_model: "gemini-2.5-flash"    # Primary AI 모델
    openai_model: "gpt-4o-mini"         # Fallback AI 모델
    timeout_seconds: 60                 # AI 응답 대기 시간 (초)

  overseas:
    prefixes: ["OT", "HG", "KL", "IM"] # 해외 예약번호 접두사 목록
    mention: "<!subteam^S03KUCG1H53>"   # 해외 건 멘션 대상 (Slack 사용자 그룹)
    reaction: "flag-jp"                 # 해외 건 리액션 이모지
```

### .env 필수 키 (결항 관련)

| 환경변수 | 용도 | 필수 여부 |
|----------|------|-----------|
| `GEMINI_API_KEY` | Gemini Flash API 키 | AI 분석 사용 시 필수 |
| `OPENAI_API_KEY` | GPT-4o-mini API 키 | Fallback 사용 시 필수 |
| `GOOGLE_CREDENTIALS_FILE` | Google 서비스 계정 인증 파일 경로 | 필수 |

### 기능 활성화 조건

```
AI 분석이 작동하려면:

  config.yaml: analysis.enabled = true
       +
  .env: GEMINI_API_KEY 또는 OPENAI_API_KEY 설정

  ┌──────────────────────────────────┬──────────────┐
  │ 조건                              │ 동작          │
  ├──────────────────────────────────┼──────────────┤
  │ enabled=false                     │ 이미지 업로드만 │
  │ enabled=true + API 키 없음        │ 이미지 업로드만 │
  │ enabled=true + Gemini 키만        │ Gemini로 분석  │
  │ enabled=true + OpenAI 키만        │ OpenAI로 분석  │
  │ enabled=true + 둘 다 있음         │ Gemini→OpenAI  │
  │                                   │ (자동 전환)    │
  └──────────────────────────────────┴──────────────┘
```

---

## 9. Slack 메시지 구성

> "봇이 Slack에 어떤 메시지를 보내는지"

### 메시지 1: 이미지 업로드

봇이 Drive에서 다운로드한 결항확인서 이미지를 스레드에 첨부합니다.

```
📎 result_1.jpg
📎 result_2.jpg
```

### 메시지 2: AI 분석 결과 (정상)

```
┌─────────────────────────────────────────────┐
│ :page_facing_up: 결항확인서 분석 결과         │
│─────────────────────────────────────────────│
│ `고객명` 홍길동          `예약번호` ABC12345  │
│ `날짜` 2026-03-18        `항공편` 7C112      │
│ `결항사유` 기상악화       `발급기관` 제주항공  │
│                                             │
│ 2026년 3월 18일 제주항공 7C112편이 기상악화로 │
│ 결항되었음을 확인하는 문서입니다.              │
└─────────────────────────────────────────────┘
```

### 메시지 3: AI 분석 결과 (부적합)

스레드에 :x: 리액션이 추가되고, 사유가 포스팅됩니다.

```
부적합 사유:
- 결항이 아닌 지연(delay) 안내 문서입니다
- 고객명이 문서에 표시되어 있지 않습니다
```

### 메시지 4: 교차검증 결과

```
교차검증: 승인 — 날짜 검증 통과

https://slack.com/archives/C0AAA/p1711012345  ← 예약 스레드 링크
```

반려/보류 시:
```
교차검증: 반려 — 결항일(2026-03-18)이 대여기간(2026-04-01~2026-04-05) 밖
```

### 리액션 정리

| 리액션 | 의미 |
|--------|------|
| :pdf: | PDF 파일이 포함된 건 |
| :x: | AI가 문서를 부적합으로 판정 |
| :flag-jp: | 해외(일본) 결항 건 |

---

## 10. 주요 파일별 함수 설명

> "각 파일이 어떤 일을 하는 함수들을 갖고 있는지"

### 서비스 계층 (app/services/)

#### cancellation_image_service.py — 전체 워크플로우 조율 (지휘자)

| 함수 | 하는 일 |
|------|---------|
| `poll_and_upload()` | 설문 시트를 폴링하고 이미지를 업로드한다 (동시 실행 방지 잠금 포함) |
| `_poll_and_upload_locked()` | 실제 폴링 로직: 미처리 건 조회 → 중복 제거 → 처리 → 완료 마킹 |
| `_process_submission(sub)` | 건 1개 처리: 스레드 찾기 → 이미지 수집 → 업로드 → 분석 → 검증 |
| `_is_overseas(booking_key)` | 예약번호 접두사로 해외 건 여부 판별 |
| `_handle_overseas(thread_ts, key)` | 해외 건 처리: 담당자 멘션 + 리액션 추가 |
| `_analyze_and_report(images, sub, ts)` | AI 분석 실행 → 결과 포스팅 → 시트 기록 → 교차검증 |
| `_verify_and_report(result, sub, ts)` | 교차검증: 예약 찾기 → 날짜 비교 → 판정 → 기록 |
| `_check_date_range(analysis, location)` | 결항일이 대여기간 ±1일 이내인지 비교 |
| `_post_bidirectional_links(ts, loc, sub)` | 승인 시 예약↔결항 스레드 간 양방향 링크 포스팅 |

#### image_analyzer.py — AI 이미지 분석 (심사관)

| 함수 | 하는 일 |
|------|---------|
| `analyze(images, submission)` | 이미지 필터링 → 프롬프트 구성 → AI 호출 → 결과 파싱 |
| `_filter_images(images)` | 4MB 초과/10장 초과 이미지 필터링 |
| `_normalize_response(raw)` | AI 응답이 배열이면 첫 번째 요소만 추출 |
| `_parse_response(raw, issues)` | JSON 응답을 AnalysisResult로 변환 |

#### drive_file_collector.py — Drive 파일 수집 (서류함 담당)

| 함수 | 하는 일 |
|------|---------|
| `collect(folder_name)` | 폴더 찾기 → 파일 목록 → 다운로드 (한번에) |
| `_download_files(files)` | 파일 다운로드 + PDF 변환, 실패 건 건너뜀 |

#### reservation_locator.py — 예약 스레드 검색 (예약 원장 담당)

| 함수 | 하는 일 |
|------|---------|
| `find(booking_key, phone)` | 예약번호로 검색 → 실패 시 전화번호로 재검색 |
| `_search(search_text)` | DB 캐시 검색 → 없으면 Slack 채널 히스토리 검색 |
| `_search_channel_by_text(channel, text)` | 채널 메시지에서 텍스트 포함 메시지 찾기 ("예약취소" 제외) |
| `_build_location(text, channel, ts)` | 찾은 메시지에서 예약 데이터 파싱 + DB 캐시 저장 |

---

### 인프라 계층 (app/infrastructure/)

#### survey_sheet.py — Google Sheets 읽기/쓰기

| 함수 | 하는 일 |
|------|---------|
| `get_all_submissions()` | 시트에서 미처리 설문 응답 목록을 가져옴 (처리완료=TRUE 제외) |
| `mark_processed(submission_id)` | 원본 시트의 처리완료 컬럼에 TRUE 기록 |
| `write_formatted_row(submission)` | 운영현황 시트에 포맷된 행 추가 (중복 체크) |
| `write_analysis_result(key, result)` | 운영현황 시트에 AI 분석 결과 기록 (결항일자, 항공편 등) |
| `write_verification_result(key, result)` | 운영현황 시트에 교차검증 결과 기록 (승인/반려/보류) |

#### drive_client.py — Google Drive 검색/다운로드

| 함수 | 하는 일 |
|------|---------|
| `find_folder(folder_name)` | 상위 폴더 아래에서 이름으로 폴더 검색 (부분일치 오매칭 방지) |
| `list_image_files(folder_id)` | 폴더 안의 이미지/PDF 파일 목록 조회 |
| `download_file(file_id)` | 파일 다운로드 (청크 단위) |

#### gemini_client.py — Gemini Flash API 호출 (주력 심사관)

| 함수 | 하는 일 |
|------|---------|
| `analyze_images(images, prompt)` | 이미지 + 프롬프트를 Gemini에 전송, JSON 응답 파싱 |

**재시도 규칙:**
- 타임아웃/연결 에러 → 1회 재시도 (2초 대기)
- Rate limit (429) → 1회 재시도 (2초 대기)
- 기타 에러 → 즉시 실패

#### openai_client.py — GPT-4o-mini Vision 호출 (보조 심사관)

| 함수 | 하는 일 |
|------|---------|
| `analyze_images(images, prompt)` | 이미지를 Base64로 인코딩하여 GPT에 전송, JSON 응답 파싱 |

#### fallback_gateway.py — Primary/Fallback 자동 전환

| 함수 | 하는 일 |
|------|---------|
| `analyze_images(images, prompt)` | Primary(Gemini) 호출 → Rate limit 에러 시만 Fallback(OpenAI) 전환 |
| `is_rate_limit_error(e)` | 에러 메시지에서 429/rate limit/quota 키워드 감지 |

**전환 규칙:**
- Rate limit → Fallback으로 전환
- 기타 에러 (인증 실패, 잘못된 요청 등) → Fallback 시도하지 않고 즉시 실패

---

### 뷰 계층 (app/views/)

#### analysis.py — Slack Block Kit 포맷

| 함수 | 하는 일 |
|------|---------|
| `build_analysis_result_blocks(result)` | AI 분석 결과를 Slack Block Kit 메시지로 변환 |

**표시 순서:** 고객명 → 예약번호 → 날짜 → 항공편 → 선편 → 결항사유 → 노선 → 출발지 → 도착지 → 발급기관

---

### 리스너 계층 (app/listener/)

#### messages.py — Slack 이벤트 감지

| 함수 | 하는 일 |
|------|---------|
| `_trigger_cancellation_poll(service, channel)` | 백그라운드 스레드에서 폴링 실행 (5초 대기 후 시작, 최대 2회 재시도) |

**트리거 조건:**
- 결항 채널에 새 메시지 도착 (부모 메시지만, 스레드 답글 제외)
- 결항 채널의 subtype 이벤트 (봇 메시지 등 — 단, message_changed/message_deleted는 제외)

---

### 컨테이너 (app/container.py) — 의존성 조립

결항 시스템이 활성화되려면 3가지 설정이 모두 있어야 합니다:

| 필수 설정 | 설명 |
|-----------|------|
| `slack_channels.target` | 결항 Slack 채널 ID |
| `drive.parent_folder_id` | Drive 상위 폴더 ID |
| `spreadsheet.id` | Google Sheets 문서 ID |

셋 중 하나라도 없으면 결항 시스템 전체가 비활성화됩니다.

**컴포넌트 조립 순서:**

```
1. SurveySheetReader   ← 설문 시트 연결
2. DriveImageClient    ← Drive API 연결
3. GeminiClient        ← (선택) AI Primary 생성
4. OpenAIImageClient   ← (선택) AI Fallback 생성
5. FallbackImageGateway ← (선택) Primary+Fallback 묶기
6. ImageAnalyzer       ← (선택) AI 분석기 생성
7. DriveFileCollector   ← Drive + PDF변환 묶기
8. ReservationLocator   ← 예약 검색기 생성
9. CancellationImageService ← 모두 조립하여 최종 서비스 생성
```
