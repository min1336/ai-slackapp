[project]
name = "slack-bot"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.13.0",
    "dotenv>=0.9.9",
    "google-auth>=2.48.0",
    "gspread>=6.2.1",
    "psycopg2-binary>=2.9.0",
    "pydantic-settings>=2.12.0",
    "pytest>=9.0.2",
    "pyyaml>=6.0.3",
    "slack-bolt>=1.27.0",
    "sqlalchemy>=2.0.0",
    "structlog>=25.1.0",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.14.13",
    "pytest>=8.0.0",
    "pytest-cov>=6.0.0",
]
[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "unit: isolated unit tests",
    "integration: component integration tests with fakes",
    "integration_real: real external integration tests (Google Sheets API)",
]
pythonpath = ["."]

[tool.ruff]
target-version = "py312"
line-length = 88


exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".git-rewrite",
    ".hg",
    ".ipynb_checkpoints",
    ".mypy_cache",
    ".nox",
    ".pants.d",
    ".pyenv",
    ".pytest_cache",
    ".pytype",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    ".vscode",
    "__pypackages__",
    "_build",
    "buck-out",
    "build",
    "dist",
    "node_modules",
    "site-packages",
    "venv"
]

[tool.ruff.lint]
select = [
    "E",
    "W",
    "F",
    "I",
    "UP",
    "B",
    "SIM",
    "C4",
    "FA",
]

ignore = [

]
[tool.ruff.lint.isort]
required-imports = ["from __future__ import annotations"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
    "pytest-cov>=6.0.0",
    "ruff>=0.14.13",
]
